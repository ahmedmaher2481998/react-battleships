import React from 'react';
import { Card, Head } from '../components';

const ResultsPage = () => {
  return (
    <>
      <Head title={'BattleShip | Results'} />
      <Card body={'this is a body '} title={'this is a title '} />
    </>
  );
};

export default ResultsPage;
